const allContacts = require('./allContacts')
const Contact = require('./Contact')
const _allContactsMeta = require('./_allContactsMeta')

module.exports = {
  allContacts,
  Contact,
  _allContactsMeta,
}
